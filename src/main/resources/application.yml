server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/akhil3kumar/configuration_properties
          default-label: main
          clone-on-start: true
